# Tasks

## Active
- `API-01` Add user CRUD endpoints (POST /users, GET /users/:id, PUT /users/:id, DELETE /users/:id) with in-memory storage. Exit criteria: all 4 endpoints work, input validation present, tests pass for each endpoint, error cases covered.

## Next Ready
- Add request logging middleware with correlation IDs
- Add rate limiting per IP

## Completed (Recent)
